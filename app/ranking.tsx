import { Feather } from '@expo/vector-icons';
import { Text, View } from 'react-native';
import { AppShell, SectionTitle } from '@/components/AppShell';
import { leaderboard } from '@/data/mockData';
export default function Ranking() { return <AppShell><View className="mb-8"><Text className="mb-2 text-[11px] font-bold uppercase tracking-[2px] text-gold">Community</Text><Text className="font-display text-4xl font-bold text-ink">Die Höhenmeter-Liga.</Text><Text className="mt-3 text-base text-moss">Jeder Aufstieg zählt. Jede Geschichte bleibt.</Text></View><View className="mb-8 flex-row gap-3"><View className="flex-1 items-center rounded-3xl bg-forest p-6"><Text className="text-3xl">🥈</Text><Text className="mt-3 text-center font-bold text-sand">Lenz_Salzburg</Text><Text className="mt-1 text-sm text-gold">14.860 hm</Text></View><View className="-mt-4 flex-1 items-center rounded-3xl border-2 border-gold bg-card p-6"><Text className="text-4xl">🏆</Text><Text className="mt-3 text-center font-bold text-ink">MountainRunner</Text><Text className="mt-1 text-sm font-bold text-gold">18.420 hm</Text></View><View className="flex-1 items-center rounded-3xl bg-card p-6"><Text className="text-3xl">🥉</Text><Text className="mt-3 text-center font-bold text-ink">Hannes_HM</Text><Text className="mt-1 text-sm text-gold">12.440 hm</Text></View></View><SectionTitle eyebrow="Saison 2025" title="Rangliste" action="Meine Position: #8" /><View className="gap-2">{leaderboard.map(person => <View key={person.rank} className="flex-row items-center gap-4 rounded-2xl border border-line bg-card p-4"><Text className="w-5 text-center font-display text-xl font-bold text-gold">{person.rank}</Text><View className="h-10 w-10 items-center justify-center rounded-full bg-forest"><Text className="text-xs font-bold text-gold">{person.initials}</Text></View><View className="flex-1"><Text className="font-bold text-ink">{person.name}</Text><Text className="mt-1 text-xs text-moss">{person.tours} Aufstiege · {person.badge}</Text></View><Text className="font-bold text-forest">{person.hm} hm</Text></View>)}</View><SectionTitle eyebrow="Deine Sammlung" title="Badges" /><View className="flex-row flex-wrap gap-3">{['Dopplersteig-König', '10k Club', 'Wald-Retter', 'Frühstarterin'].map((badge, index) => <View key={badge} className="min-w-[45%] flex-1 rounded-2xl border border-line bg-card p-4"><Feather name={index === 2 ? 'heart' : 'award'} size={20} color={index === 2 ? '#D9A036' : '#1E382B'} /><Text className="mt-4 font-bold text-ink">{badge}</Text><Text className="mt-1 text-xs text-moss">{index < 3 ? 'freigeschaltet' : 'noch 2 Aufstiege'}</Text></View>)}</View></AppShell>; }
